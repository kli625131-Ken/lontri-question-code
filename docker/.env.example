# Copy this file to docker/.env and replace all placeholder secrets before production use.

COMPOSE_PROJECT_NAME=problem-platform

MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_DATABASE=problem_db
MYSQL_USER=problem_user
MYSQL_PASSWORD=change_me_app_password

JWT_SECRET=change_me_jwt_secret_at_least_32_characters
APP_DEFAULT_PASSWORD=change_me_default_password

TZ=Asia/Shanghai
